type: object
description: A challenge platform
allOf:
  - $ref: ChallengePlatformCreateResponse.yaml
  - $ref: ChallengePlatformCreateRequest.yaml
  - type: object
    properties:
      createdAt:
        description: When this challenge platform has been created
        type: string
        format: date-time
      updatedAt:
        description: When this challenge platform has last been updated
        type: string
        format: date-time
    required:
      - createdAt
      - updatedAt
