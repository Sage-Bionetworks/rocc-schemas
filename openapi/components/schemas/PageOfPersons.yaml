type: object
description: A page of persons.
allOf:
  - $ref: ResponsePageMetadata.yaml
  - type: object
    properties:
      persons:
        description: An array of persons.
        type: array
        items:
          $ref: Person.yaml