type: object
description: An object that describes how to filter Persons
properties:
  firstName:
    description: Keep the persons whose first name starts with this term
    type: string
  lastName:
    description: Keep the persons whose last name starts with this term
    type: string
  email:
    description: Keep the persons whose email matches this term
    type: string
  organization:
    description: Keep the persons who belong to this organization
    type: string
  # organizations:
  #   description: Keep the persons who belong to these organizations
  #   type: string
    # TODO: Figure out why the following does not work
    # See https://github.com/Sage-Bionetworks/rocc/issues/36
    # type: array
    # items:
    #   $ref: ../parameters/OrganizationId.yaml