name: sort
description: Property used to sort the results that must be returned
in: query
required: false
schema:
  type: string
  enum:
    - featured
    - numberOfParticipants
    - numberOfViews
    - numberOfStargazers
    - alphabetical
    - startDate
    - createdAt
    - updatedAt
